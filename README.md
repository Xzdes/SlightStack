# SlightStack — Реактивный UI-фреймворк с CSS-подобным API

**SlightStack** — это легковесный JavaScript-фреймворк, который объединяет простоту HTML-компонентов с мощью декларативного JS API. Забудьте о сложных CSS-in-JS библиотеках. Вместо этого описывайте стили, состояния и адаптивность прямо в свойствах компонентов, используя интуитивный синтаксис, вдохновленный Tailwind CSS и CSS-псевдоклассами.

Он сочетает простоту классических шаблонов, удобный **JS Builder API** для их сборки и высокопроизводительный **"keyed" Virtual DOM** для молниеносных обновлений.

**Ключевые особенности:**

*   🧱 **HTML как компонент:** Ваш главный инструмент — это HTML. Создайте папку, положите в нее `component.html` и `component.css` — и новый компонент готов к использованию.
*   ✨ **CSS-подобный API в JS:** Управляйте стилями, состояниями (`:hover`) и адаптивностью (`sm:`) прямо в JavaScript. Декларативно, интуитивно и в одном месте.
*   🚀 **Производительный Virtual DOM:** Молниеносные обновления списков и интерфейса благодаря эффективному "keyed" алгоритму сравнения (diff/patch), который минимизирует манипуляции с DOM.
*   ♻️ **Композиция через `{{SLOT}}`:** Вкладывайте компоненты друг в друга с помощью простого плейсхолдера `{{SLOT}}`, создавая сложные иерархии без лишних усилий.
*   🔗 **Двустороннее связывание:** Используйте `.model()` для легкой синхронизации состояния с полями ввода (`input`, `checkbox`), сокращая шаблонный код до одной строки.
*   ⚡ **Нулевая конфигурация:** Никаких сложных сборщиков. Просто создайте папку для компонента и начните творить. Система автоматически подхватит его.
*   🖥️ **Встроенный DevServer:** Одна команда для запуска демо-проекта с автоматической пересборкой при изменениях.

---

## 🚀 Быстрый старт

### 1. Установка
Убедитесь, что у вас установлен [Node.js](https://nodejs.org/) (версия 18+).

```bash
# Клонируйте репозиторий (если еще не сделали)
# git clone https://github.com/Xzdes/SlightStack.git

# Перейдите в папку проекта
cd slightstack

# Установите все необходимые зависимости
npm install
```

### 2. Запуск демо-приложения
Для начала работы выполните команду в терминале:

```bash
npm run start:demo
```

Эта команда запустит локальный DevServer для демо-приложения. После успешного запуска вы увидите в консоли:

```
✅ SlightStack Modular Demo запущено на http://localhost:3000
```
**Откройте в браузере адрес `http://localhost:3000`**, и вы увидите работающее приложение "To-Do List", которое демонстрирует все основные возможности фреймворка.

---

## 🛠️ Основные концепции

### 1. Компонент — это просто папка

В SlightStack компонент — это папка внутри `hybrid-components/`.

**Пример: `hybrid-components/button/component.html`**
```html
<button class="slight-button">{{TEXT}}</button>
```
**`hybrid-components/button/component.css`**
```css
.slight-button {
  padding: 8px 16px;
  border-radius: 6px;
  /* ... другие стили ... */
}
```
Фреймворк автоматически найдет этот компонент и сделает доступным `UI.button`. Плейсхолдер `{{TEXT}}` можно будет заполнить через JavaScript.

### 2. Сборка интерфейса: Декларативность — это всё

Вместо написания сложных объектов стилей, вы описываете внешний вид компонентов прямо в их свойствах.

```javascript
// Файл: demo/app.js
module.exports = function(UI) {

    // Простая кнопка с текстом
    const myButton = UI.button({ text: 'Нажми меня' });
    
    // Кнопка с адаптивностью и состояниями
    const myStyledButton = UI.button({
        text: 'Я меняюсь!',
        background: 'blue',
        color: 'white',

        // Адаптивность: на маленьких экранах (< 768px)
        'sm:padding': '8px',
        'sm:text': 'ОК',

        // Состояния: при наведении
        ':hover:background': 'darkblue'
    });
};
```

### 3. Декларативная Адаптивность

Забудьте о медиа-запросах в CSS. Управляйте стилями для разных размеров экрана с помощью префиксов `sm:`, `md:`, `lg:`.

```javascript
// Настройте свои брейкпойнты один раз в app.js
UI.config({
    breakpoints: {
        sm: 768, // до 768px
        md: 1024, // от 768px до 1024px
        // lg - всё, что больше
    }
});

// Используйте их где угодно
const adaptiveLayout = () => 
    UI.row({
        // По умолчанию - колонка, на экранах > 768px - ряд
        flexDirection: 'column',
        'sm:flexDirection': 'row',

        gap: 10,
        'sm:gap': 20
    });
```

### 4. Интерактивные Состояния

Управляйте стилями для `:hover`, `:focus`, `:disabled` и других состояний так же просто, как и адаптивностью.

```javascript
const interactiveInput = () =>
    UI.input({
        border: '1px solid #ccc',
        transition: 'all 0.2s',

        // При фокусе меняем рамку и добавляем тень
        ':focus:border': '1px solid blue',
        ':focus:boxShadow': '0 0 5px rgba(0,0,255,0.5)',

        // Стили для неактивного состояния
        ':disabled:background': '#eee',
        ':disabled:cursor': 'not-allowed'
    });
```
Вы даже можете комбинировать их: `'sm:hover:background': 'red'`!

### 5. Композиция с помощью `{{SLOT}}`

Чтобы вкладывать компоненты друг в друга, используйте плейсхолдер `{{SLOT}}`.

**Пример: `hybrid-components/stack/component.html`**
```html
<div class="slight-stack">
  {{SLOT}} <!-- Сюда будут вставлены дочерние компоненты -->
</div>
```

**Использование в `app.js`:**
```javascript
const AppView = () => (
    UI.stack().children(
        UI.text({ text: 'Заголовок' }),
        UI.button({ text: 'Кнопка внутри стека' })
    )
);
```

### 6. Реактивность и состояние

Используйте `UI.createReactive` для создания объекта состояния. Фреймворк будет автоматически отслеживать его изменения и перерисовывать только нужные части UI.

```javascript
const state = UI.createReactive({
    counter: 0
});

const CounterView = () => (
    UI.text({ text: `Счетчик: ${state.counter}` })
);
```

### 7. Рендеринг списков (`UI.for`)

Для рендеринга динамических списков используйте `UI.for`. **Ключевое правило:** всегда передавайте в `each` функцию, возвращающую массив, и всегда указывайте `key`.

```javascript
const AppView = () => (
    UI.stack().children(
        UI.for({
            each: () => state.tasks, // Передаем функцию!
            key: 'id', // Уникальный ключ для каждого элемента
            as: (task) => UI.text({ text: task.title, key: task.id })
        })
    )
);
```

### 8. Двустороннее связывание (`.model()`)

Сократите код для работы с формами до одной строки с помощью хелпера `.model()`.

```javascript
const state = UI.createReactive({
    name: '',
    isAdmin: false
});

const FormView = () => (
    UI.stack().children(
        // Связываем с текстовым полем
        UI.input({ placeholder: 'Ваше имя' })
            .model(state, 'name'),
        
        // Связываем с чекбоксом
        UI.input()
            .model(state, 'isAdmin')
    )
);
```
`.model()` автоматически определит тип поля и привяжет нужные свойства (`value`/`checked`) и события (`input`/`change`).

---

## 📂 Структура проекта

`SlightStack` спроектирован для простоты и ясности. Ядро фреймворка теперь полностью модульное.

```
slightstack/
├── core/                   # Модульное ядро фреймворка
│   ├── dom/                #   - Взаимодействие с DOM (создание, патчинг)
│   ├── reactivity/         #   - Реактивность (Proxy, эффекты)
│   ├── renderer/           #   - Логика VDOM (mount, unmount, patch)
│   ├── vdom/               #   - Структура VNode (создание, нормализация)
│   ├── api.js              #   - Публичный JS Builder API (UI.button и т.д.)
│   ├── create-app.js       #   - Главная функция сборки приложения
│   └── state-manager.js    #   - Управление состоянием окна и брейкпойнтами
│
├── demo/                   # Папка с рабочим демо-приложением
│   ├── app.js
│   └── server.js           # DevServer и сборщик
│
├── hybrid-components/      # ВАША ГЛАВНАЯ ПАПКА: здесь живут ваши HTML/CSS компоненты
├── index.js                # Публичное API пакета для сборщиков
└── ...
```

---

## 🔮 Философия и что дальше?

**SlightStack** доказывает, что современные, реактивные интерфейсы можно создавать, не отказываясь от простоты и мощи чистого HTML. Он создан для быстрой разработки, прототипирования и для тех, кто ценит прозрачность и контроль над своим кодом.

**Что дальше?**
*   **Изоляция CSS (Scoping):** Сейчас CSS для всех гибридных компонентов глобальный. Это просто и работает для небольших проектов, но в крупных приложениях может привести к конфликтам стилей. Следующий логичный шаг — внедрение автоматического скоупинга. Самый простой способ — при парсинге компонента добавлять к каждому классу в CSS и к каждому `class` в HTML уникальный data-атрибут (например, `data-slight-button-xyz`). Это полностью инкапсулирует стили компонента.
*   **Расширяйте библиотеку:** Создавайте новые компоненты в `hybrid-components/`. Попробуйте воссоздать элементы с [uiverse.io](https://uiverse.io/elements) или любого другого ресурса.
*   **Создайте визуальный редактор:** Архитектура `SlightStack` идеально подходит для создания "drag-and-drop" редактора.


Отличный вопрос! После того как мы стабилизировали ядро и реализовали изоляцию CSS, у нас остался один, но самый интересный пункт из нашего "идеального плана". Это фича, которая выведет удобство использования (Developer Experience) на совершенно новый уровень.

### Осталось реализовать: **Расширение системы модификаторов (Мощь)**

**Текущее состояние:**
Сейчас у нас есть два типа модификаторов:
*   **Брейкпойнты:** `sm:padding`, `md:color`
*   **Состояния:** `:hover:background`, `:focus:border`

**Что мы хотели добавить:**

1.  **Псевдо-классы для дочерних элементов:** Возможность стилизовать первый, последний или каждый N-ный элемент в списке, не выходя из JS.
2.  **Псевдо-элементы:** Возможность стилизовать `::before` и `::after`.
3.  **Групповой ховер:** Возможность менять стиль дочернего элемента, когда наведен курсор на родительский.

Давайте сфокусируемся на самом полезном и эффектном из этого списка.

---

### **План реализации "Расширения системы модификаторов"**

#### **1. Псевдо-классы для дочерних элементов (`:first`, `:last`, `:even`, `:odd`)**

Это самая востребованная фича для работы со списками.

**Как будет выглядеть:**
```javascript
UI.for({
    each: () => state.items,
    key: 'id',
    as: (item) => UI.item({
        text: item.name,
        borderBottom: '1px solid #ccc',
        // Убираем рамку у последнего элемента
        ':last:borderBottom': 'none'
    })
})
```

**Как реализовать:**
Это потребует изменений в `core/vdom/props-resolver.js` и, возможно, в `core/renderer/patch.js`.

*   **Идея А (Простая):** `patchChildren` при обходе списка может добавлять специальный класс или data-атрибут (`data-is-first`, `data-is-last`) к соответствующим элементам. А `props-resolver` будет генерировать CSS-правила, нацеленные на эти атрибуты.
*   **Идея Б (Сложная, но мощная):** Научить `props-resolver` понимать, что `:last:borderBottom` — это не инлайн-стиль, а CSS-правило, и динамически генерировать тег `<style>` с правилом вроде `[data-slight-v-xyz]:last-child { border-bottom: none; }`.

#### **2. Групповой ховер (`:group-hover`)**

Это вторая по полезности фича, позволяющая создавать сложные интерактивные карточки.

**Как будет выглядеть:**
```javascript
// Родительский элемент помечается как группа
UI.row({ group: true }).children(
    UI.avatar({ src: '...' }),
    UI.text({ text: 'Имя' }),
    UI.button({
        text: 'Удалить',
        opacity: 0,
        // Показываем кнопку, когда курсор наведен на весь UI.row
        ':group-hover:opacity': 1
    })
)
```

**Как реализовать:**
1.  **`state-manager.js`:** `attachInteractiveState` нужно научить искать проп `group: true`. Если он есть, то `mouseenter`/`mouseleave` на этом элементе должны не только менять его собственный `isHovering`, но и добавлять/убирать `data-group-hover="true"` с DOM-элемента.
2.  **`props-resolver.js`:** Он должен научиться генерировать CSS-правило. Когда он видит `':group-hover:opacity': 1` для компонента с `scopeId="data-v-123"`, он должен создать CSS-правило: `[data-group-hover="true"] [data-v-123] { opacity: 1; }`.

#### **3. Псевдо-элементы (`:before`, `:after`)**

Это менее приоритетная, но все еще очень полезная фича.

**Как будет выглядеть:**
```javascript
UI.label({
    text: 'Обязательное поле',
    // Добавляем красную звездочку после текста
    ':after:content': '" *"',
    ':after:color': 'red'
})
```
**Как реализовать:**
Аналогично групповому ховеру. `props-resolver` должен будет генерировать CSS-правила для `[data-v-xyz]::after { content: " *"; color: red; }`.

---

**Итог, что осталось:**

Реализация этих трех пунктов превратит ваш CSS-подобный API из "очень хорошего" в **"лучший в своем классе"**. Он станет невероятно мощным и выразительным, позволяя создавать сложный и интерактивный UI с минимальным количеством кода.

Начать стоит с **псевдо-классов для детей**, так как это самая частая задача в работе со списками. Затем — **групповой ховер**, так как он открывает дорогу к сложным компонентам. И в конце — **псевдо-элементы** как вишенка на торте.